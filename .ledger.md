`npm audit`: 0 vulnerabilities. No janitor PR left red from last night.

Backlog first. Nine `janitor` issues carried over; most are still blocked on a decision this run cannot take: #873 and #859 need a message-to-mailbox relation that does not exist, #585 and #497 name two alternatives and pick neither, #314 waits on three unlanded slices. Those stay open.

Four are workable tonight, on disjoint file sets:

- [ ] #951 — a send that never reaches the SMTP server strands the outbox row at `sending`.
- [ ] #876 — a delete inside a name-guessed Trash expunges anyway.
- [ ] #723 — one `j` press moves the list cursor and the conversation cursor.
- [ ] #916 — Folder roles claims an exclusivity the backend does not enforce.

Held for a later wave, because they sit next to wave 1's files or need verifying first:

- [ ] #946 — regression test for deleting the brief's open message.
- [ ] #347 — `delete-folder.spec.ts` flake.
- [ ] #845 — audit of ten mail-losing paths; needs splitting before it is actionable.
