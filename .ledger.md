Rung 0: `npm audit` clean. #999 and #1006 were green and unmerged from last night — both merged. #1000 conflicts with main and needs a rebase.

## Wave 1

- [ ] #1000 rebase the delimiter-labels branch onto main (rung 0)
- [ ] #1004 two `ApiError` types share a name, so the appointment prompt never opens
- [ ] #1013 the outbox endpoint lists only the first account
- [ ] #1015 the update banner overlays the content under it

## Held

- #1002, #1016 — both need an API operation or response field added, so both need owner sign-off before implementation.
- #873, #859, #585 — each needs a product or schema decision.
- #314 — waits on #308 and #312.
