Static asset branch: Storybook screenshots for PR #49 (`@ui` global top bar and
search chip input). Not for merge — the images are referenced from the PR body
and this branch exists only so those URLs keep resolving.
