function i(t){return t.scopedOutFolder!==void 0||(t.droppedFacets?.length??0)>0||t.droppedSemantic===!0}function s(t){return`Your search was limited to ${t}. This filter matches these messages in any folder — it can't be pinned to one.`}const r=t=>t.length===1?t[0]:t.length===2?`${t[0]} and ${t[1]}`:`${t.slice(0,-1).join(", ")} and ${t[t.length-1]}`;function l(t){const e=t.length===1?"a filter condition":"filter conditions",n=t.length===1?"isn't":"aren't";return`${r(t)} ${n} ${e}, so ${t.length===1?"it was":"they were"} left out — the filter still matches everything else you searched for.`}const o="add a sender or words to filter on";function a(t){if(t.length===0)return`${o[0].toUpperCase()}${o.slice(1)}`;const e=t.length===1?"isn't":"aren't",n=t.length===1?"a filter condition":"filter conditions";return`${r(t)} ${e} ${n} — ${o}`}const h="The filter matches these words literally. Your search also found similar mail by meaning — a filter can't carry that, so it won't catch mail that means the same thing without these words.";export{h as D,l as d,i as h,a as m,s};
